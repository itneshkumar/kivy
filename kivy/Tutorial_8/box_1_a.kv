<MyLayout>
    BoxLayout:
        orientation: "vertical"
        #orientation: "horizontal"
        size: root.width, root.height
        #padding
        padding: 50
        # spacing
        spacing: 20

        Button:
            text: "Hello world!"
        Button:
            text: "Goodbye world!"
        Button:
            text: "I am hungry"
            pos_hint:{'center_x':.5}
            #size_hint:(0.5,1)
            size_hint:(1,.1)
            # we can directly use height and width
            #pos_hint:{'center_x':.5}
            #size_hint:(None,None)
            #width: 100
            #height: 40

