MDBoxLayout:
    orientation: 'vertical'

    MDScreen:
        MDLabel:
            id: my_label
            text: 'some stuff'
            halign: 'center'
        MDFloatingActionButtonSpeedDial:
            data: app.data
            root_button_anim: True
            # COLOR STUFF
#            label_text_color: 1,0,0,1
#            bg_color_stack_button: 1,0,0,1 #bg of icon
#            bg_color_root_button: 1,0,0,1 #speed dial button bg
#            color_icon_rooot_button: 1,0,0,1 #speed dial text color
            color_icon_stack_button: 1,0,0,1 # icon text color
#            bg_hint_color: 1,0,0,1 # change the background icon color
            #pushing the button
            callback: app.callback
            # open or close
            on_open: app.open()
            on_close: app.close()
